name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  go:
    name: Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - uses: golangci/golangci-lint-action@v9
        with:
          # v2.13+ is required for formatters.settings.gofumpt.extra
          # (group-params / clothe-returns / balance-calls). v2.5.0
          # only accepts extra-rules and fails `config verify`.
          version: v2.13.2
          args: --timeout=3m
      - name: Test
        run: make test
      - name: Build
        run: make build

  solidity:
    name: Solidity
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: solidity
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: solidity/package-lock.json
      - name: Install
        run: npm ci
      - name: Test
        run: npm test
